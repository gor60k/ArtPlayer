AlbumArtWallpaper/
│
├─ AlbumArtWallpaperApp.swift
│
├─ AppDelegate/
│  └─ AppDelegate.swift
│
├─ MenuBar/
│  ├─ MenuController.swift
│  └─ MenuActions.swift
│
├─ Players/
│  ├─ PlayerType.swift
│  ├─ SpotifyPlayer.swift
│  └─ AppleMusicPlayer.swift
│
├─ AppleScript/
│  ├─ AppleScriptRunner.swift
│  └─ Scripts.swift
│
├─ Wallpaper/
│  └─ WallpaperManager.swift
│
├─ Network/
│  └─ ImageDownloader.swift
│
├─ Models/
│  └─ TrackArtwork.swift
│
├─ Utils/
│  ├─ Logger.swift
│  └─ Constants.swift
│
├─ Resources/
│  ├─ Assets.xcassets
│  └─ Info.plist
│
└─ Preview Content/
   └─ Preview Assets.xcassets
   
       if application "Spotify" is running then
        tell application id "com.spotify.client"
            try
                set theURL to (get artwork url of current track)
                return theURL as string
            on error
                return ""
            end try
        end tell
    else
        tell application id "com.spotify.client" to launch
        return ""
    end if
    
    
    как мне правильно обработать position и duration текущего трека, который я получаю из плеера?

вот остальные данные трека

let track = result.atIndex(1)?.stringValue ?? "Неизвестно"
        let artist = result.atIndex(2)?.stringValue ?? "-"
        let state = result.atIndex(3)?.stringValue ?? "paused"
        let isPlaying = state.lowercased().contains("play") || state.contains("kPPl")

вот так выглядит вывод результата выполнения apple script

это инфо трека <NSAppleEventDescriptor: [ 'utxt'("Eren Zahyo"), 'utxt'("Sawano Hiroyuki"), 'utxt'("playing"), 'utxt'("0,094999998808"), 383120 ]>
